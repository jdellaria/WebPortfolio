import follow from './follow';
import client from './client';

console.log(follow(client , '/api/nlusums/search' ,{rel: this.state.searchBy, params: {size: pageSize, sort:"year,desc", itemtext: itemTextUpdate}}] ));
